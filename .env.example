# Owner (user or org) for GitHub repositories
GH_OWNER=your-gh-user-or-org

# Visibility flag passed to gh repo create (private or public)
VISIBILITY=private

# Set to true to let the bootstrap script commit and push automatically
AUTO_COMMIT=false
